# 발행 대시보드 사용법

Quartz Publish 플러그인의 발행 대시보드를 사용하여 노트의 발행 상태를 확인하고 일괄 관리하는 방법을 안내합니다.

## 대시보드 열기

다음 방법 중 하나로 대시보드를 열 수 있습니다:

1. **커맨드 팔레트**: `Cmd/Ctrl + P` → "Quartz Publish: Open Dashboard" 검색
2. **사이드바 아이콘**: 좌측 사이드바의 Quartz Publish 아이콘 클릭
3. **단축키**: 설정에서 지정한 단축키 사용

## 대시보드 구성

대시보드는 크게 4개의 섹션으로 구성됩니다:

```
┌─────────────────────────────────────────┐
│  Quartz Publish Dashboard               │
├─────────────────────────────────────────┤
│  [Sync All]  [Refresh]  [Apply]         │
├─────────────────────────────────────────┤
│  📋 New (3)                             │
│  ├─ ☐ 새 노트 1.md                      │
│  ├─ ☐ 새 노트 2.md                      │
│  └─ ☐ posts/새 노트 3.md                │
├─────────────────────────────────────────┤
│  ✏️ Modified (2)                        │
│  ├─ ☐ 수정된 노트 1.md                  │
│  └─ ☐ 수정된 노트 2.md                  │
├─────────────────────────────────────────┤
│  🗑️ Deleted (1)                         │
│  └─ ☐ 삭제된 노트.md                    │
├─────────────────────────────────────────┤
│  ✅ Up to date (5)                      │
│  └─ (최신 상태 노트 목록)                │
└─────────────────────────────────────────┘
```

## 노트 상태 이해하기

### 🆕 New (신규)

로컬 Obsidian에는 있지만 원격 Quartz 리포지토리에는 없는 노트입니다.

**조건**:
- `publish: true` 프론트매터가 있음
- 원격 리포지토리에 해당 파일이 없음

**동작**:
- 발행 시 리포지토리에 새 파일 생성

### ✏️ Modified (수정됨)

로컬에서 수정되어 원격과 내용이 다른 노트입니다.

**조건**:
- `publish: true` 프론트매터가 있음
- 원격 리포지토리에 파일이 존재함
- 로컬 내용과 원격 내용이 다름

**동작**:
- 발행 시 원격 파일 업데이트

### 🗑️ Deleted (삭제됨)

로컬에서 삭제되었지만 원격 리포지토리에는 남아있는 노트입니다.

**조건**:
- 원격 리포지토리에 파일이 존재함
- 로컬에 해당 파일이 없음 (또는 `publish: false`로 변경됨)

**동작**:
- 삭제 시 원격 파일 제거

### ✅ Up to date (최신 상태)

로컬과 원격이 동일한 상태의 노트입니다.

**조건**:
- `publish: true` 프론트매터가 있음
- 원격 리포지토리에 파일이 존재함
- 로컬 내용과 원격 내용이 동일함

**동작**:
- 추가 작업 불필요

## 기본 사용법

### 개별 노트 발행

1. 발행할 노트 옆의 체크박스 선택
2. **Apply** 버튼 클릭
3. 발행 진행 상황 확인
4. 완료 알림 확인

### 전체 동기화

모든 변경사항을 한 번에 처리합니다:

1. **Sync All** 버튼 클릭
2. 확인 모달에서 변경 내역 확인
3. **Confirm** 클릭
4. 동기화 진행 상황 확인

**동기화 시 처리되는 항목**:
- New → 발행
- Modified → 업데이트
- Deleted → 삭제

### 상태 새로고침

**Refresh** 버튼을 클릭하여 최신 상태로 갱신합니다.

> 대시보드는 열릴 때 자동으로 상태를 가져옵니다. 외부에서 변경이 있었다면 Refresh로 갱신하세요.

## 일괄 작업

### 여러 노트 선택

각 섹션의 노트 옆 체크박스를 사용하여 원하는 노트를 선택합니다.

**섹션 전체 선택**: 섹션 헤더의 체크박스 클릭

### 선택 노트 발행

1. New 또는 Modified 섹션에서 노트 선택
2. **Apply** 버튼 클릭
3. 선택한 노트만 발행됨

### 선택 노트 삭제

1. Deleted 섹션에서 노트 선택
2. **Apply** 버튼 클릭
3. 확인 모달에서 삭제 확인
4. 원격 리포지토리에서 삭제됨

## 충돌 처리

로컬과 원격 모두에서 변경이 있을 경우 충돌 모달이 표시됩니다.

### 충돌 해결 옵션

| 옵션 | 설명 |
|------|------|
| **Keep Local** | 로컬 버전으로 원격 덮어쓰기 |
| **Keep Remote** | 원격 버전 유지, 로컬 변경 무시 |
| **Cancel** | 작업 취소 |

> **권장**: 중요한 변경이 있다면 먼저 로컬에 백업 후 진행하세요.

## 진행 상황 표시

발행/동기화 진행 시 프로그레스 바가 표시됩니다:

```
Publishing notes... [████████░░░░░░░░] 50%
3 of 6 notes published
```

### 진행 상황 정보

- 현재 처리 중인 노트명
- 전체 대비 진행률 (%)
- 완료된 노트 수 / 전체 노트 수

## 키보드 단축키

대시보드 내에서 사용할 수 있는 키보드 단축키:

| 키 | 동작 |
|----|------|
| `Enter` | 선택한 노트 발행/삭제 |
| `Escape` | 대시보드 닫기 |
| `Space` | 포커스된 노트 선택/해제 |
| `↑` / `↓` | 노트 목록 탐색 |

## 오류 처리

### 네트워크 오류

오프라인 상태에서는 대시보드가 제한된 기능만 제공합니다:

- 로컬 노트 목록 확인 가능
- 발행/동기화 버튼 비활성화
- "오프라인 상태" 메시지 표시

네트워크 연결 후 **Refresh** 버튼을 클릭하세요.

### Rate Limit 오류

GitHub API 제한에 도달하면:

- 오류 메시지와 재시도 가능 시간 표시
- 지정된 시간 후 다시 시도

### 대용량 파일 경고

10MB를 초과하는 파일 발행 시:

- 경고 모달 표시
- 계속 진행 또는 취소 선택 가능

> 대용량 이미지는 압축하거나 외부 서비스 사용을 권장합니다.

## 팁과 권장사항

### 효율적인 워크플로우

1. **정기적인 동기화**: 작업 세션 시작/종료 시 Sync All 실행
2. **선택적 발행**: 완성된 노트만 선택하여 발행
3. **상태 확인**: 발행 전 Refresh로 최신 상태 확인

### 대량 노트 관리

많은 노트를 관리할 때:

- 폴더별로 나누어 발행
- `draft: true`로 미완성 노트 표시
- 정기적으로 Deleted 섹션 정리

### 백업 권장

중요한 변경 전:

1. 로컬 볼트 백업
2. Git에서 현재 상태 커밋 확인
3. 충돌 가능성 있는 노트 확인

## 문제 해결

### 상태가 업데이트되지 않음

1. **Refresh** 버튼 클릭
2. 네트워크 연결 확인
3. GitHub Token 유효성 확인
4. 플러그인 설정에서 Test Connection 실행

### 특정 노트가 표시되지 않음

1. 프론트매터에 `publish: true` 있는지 확인
2. 파일이 `.md` 확장자인지 확인
3. 파일 경로에 특수문자가 없는지 확인

### 발행 후에도 Modified로 표시됨

1. **Refresh** 버튼 클릭
2. 로컬 파일이 저장되었는지 확인
3. 발행 시 자동 변환된 내용이 있을 수 있음

## 참고 문서

- [플러그인 사용 가이드](plugin-usage-guide.md) - 전체 기능 안내
- [Quartz 설정 관리 가이드](quartz-settings-guide.md) - 설정 관리
- [Quartz 블로그 만들기 가이드](quartz-blog-setup-guide.md) - 초기 설정
